# T4.1 — OpenAPI/Scalar docs + error & pagination polish

**Branch:** `feat/T4.1-api-docs-polish`  ·  **Depends on:** T2.5  ·  **Status:** 🟡 In progress (PR #22)

## Context
Make the API presentable and consistent for reviewers: a live OpenAPI/Scalar docs page (a mandated
deliverable), uniform problem-details errors, and consistent pagination/filtering across lists.

## Progress (same branch — do not merge until card is complete)
| Step | Scope | Status |
|------|-------|--------|
| 1 | OpenAPI + Scalar UI, bearer auth, endpoint metadata | ✅ PR #22 (merged pending remaining steps) |
| 2 | ProblemDetails uniformity | ⬜ follow-up on `feat/T4.1-api-docs-polish` |
| 3 | Pagination/filter contract on all list endpoints | ⬜ follow-up on `feat/T4.1-api-docs-polish` |
| 4 | `/health` + request logging/correlation | ⬜ follow-up on `feat/T4.1-api-docs-polish` |

**Merge policy:** This card stays **open** until steps 2–4 land on the same branch. Step 1 may merge
incrementally only if the team explicitly accepts partial delivery; default is complete the card first.

## Scope
**In:**
- Built-in OpenAPI document (`Microsoft.AspNetCore.OpenApi`) + **Scalar UI** at a documented path
  (the "Swagger / API documentation URL" deliverable).
- Consistent `application/problem+json` errors via centralized exception handling.
- Uniform pagination/filtering contract across all list endpoints; document auth in OpenAPI (bearer).
- Health endpoint surfaced; basic request logging/correlation if not already present.

**Out:** new features; deployment (T4.3); securing/disabling docs (intentionally exposed for the assessment).

## References
- HLD §2 (OpenAPI + Scalar), §8 (problem-details, pagination). Brief: Swagger/API docs URL deliverable.
- `docs/best-practices/aspnetcore-best-practice-rules.md` (§7–8 problem details, §22–23 pagination, §17 logging).

## Roadmap (high-level)
1. Wire OpenAPI document generation + Scalar UI; include bearer auth scheme + endpoint metadata.
2. Centralize exception handling → consistent ProblemDetails; remove ad-hoc try/catch.
3. Normalize the pagination/filter contract across list endpoints.
4. Confirm `/health` + structured request logging.

## Acceptance Criteria (Definition of Done — also workflow.md §6)
- [ ] Build/format/test clean.
- [x] Docs UI reachable and shows all endpoints with auth; OpenAPI doc valid. *(step 1)*
- [ ] Errors are uniform ProblemDetails; lists share one pagination/filter shape.

## Test Expectations
- Integration: a representative error returns ProblemDetails with correct status; a list endpoint honors paging params; the OpenAPI doc endpoint responds.

## Best-Practices Checklist (subset)
- [ ] Centralized errors; ProblemDetails (aspnetcore §7–8).
- [ ] Pagination on all lists; structured logging; no secrets in logs (aspnetcore §22–23,17; security §18).
