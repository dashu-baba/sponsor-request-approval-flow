# T4.4 — Docs finalization

**Branch:** `docs/T4.4-final-docs`  ·  **Depends on:** T4.3  ·  **Status:** ⬜

## Context
Complete the mandated documentation deliverables so a reviewer can run, test, and understand the
system end-to-end, and reach the live environment.

## Scope
**In:**
- `README.md`: finalize setup guide — run backend, run frontend, DB setup, **test login accounts**
  (from T1.3), and a Live URLs section (frontend, API, docs/Scalar, repo link, deployment notes).
- **Configuration / environment variables:** add a short **Configuration** subsection to `README.md`
  summarizing the key vars and **clearly marking secrets**, then linking to the canonical reference.
  - **Decision:** `docs/deploy.md` "Environment Setup" stays the **canonical, detailed** env reference
    (deployment mechanics = T4.3); the README subsection is a concise summary + pointer to `deploy.md`
    and `.env.example` (avoids two drifting copies).
  - Vars to cover (from `.env.example`): **infra/compose** — `COMPOSE_ENV_FILE`,
    `ASPNETCORE_ENVIRONMENT`, `POSTGRES_DB/USER/PASSWORD`, `MINIO_ROOT_USER/PASSWORD`; **app settings
    (`__` binding)** — `ConnectionStrings__Default`, `Minio__Endpoint/AccessKey/SecretKey/BucketName`,
    `Jwt__Issuer/Audience/SigningKey/AccessTokenLifetimeMinutes/RefreshTokenLifetimeDays`. Secrets
    (`POSTGRES_PASSWORD`, `MINIO_ROOT_PASSWORD`, `Minio__SecretKey`, `Jwt__SigningKey`) flagged with a
    "never commit a real `.env`" note. Suggested table columns: Variable · Scope · Purpose ·
    Example/default · Secret?
- **Diagrams (Mermaid, embedded in markdown — not images/PlantUML;** GitHub renders natively, diffable,
  no binary assets to go stale):
  - **Request lifecycle / workflow** (`stateDiagram-v2`) — `Draft → PendingManagerApproval →
    PendingFinanceReview → Approved`, plus `Reject→Rejected` and `Cancel→Cancelled`; annotate actor
    role per transition and no-self-approval (B4). → HLD workflow section + README architecture.
  - **Auth flow** (`sequenceDiagram`) — login → access token (in-memory) + refresh cookie (httpOnly,
    `Path=/api/auth`); 401 → silent refresh → retry; password change → SecurityStamp invalidates old
    tokens; logout. → HLD auth section.
  - **Deployment topology** (`flowchart`) — Browser → nginx (serves SPA, strips `/api`) → Kestrel API
    → Postgres + MinIO. → README architecture + `deploy.md`.
  - *Optional:* **Clean Architecture layers** (`flowchart`) — Api → Application → Domain ←
    Infrastructure (dependency direction). → HLD.
  - RBAC stays a **table** (role × capability), not a diagram.
- Architecture explanation: ensure `docs/high-level-design.md` (+ clarifications) cover backend
  architecture, frontend structure, workflow logic, RBAC, DB design, assumptions/tradeoffs — update
  to match the built system (note as-built deviations: `/api` prefix + nginx strip (T4.5),
  SecurityStamp token invalidation (T5.1), admin user management (T5.2), OpenAPI/Scalar + bearer
  scheme (T4.1), one-role-per-user (B6)).
- Cross-check all doc links; ensure backlog reflects any remaining deferred items (incl. resolving
  the **B-030 ID collision** between PR #22 and the merged "integration helpers" entry).

**Out:** code/features; deployment mechanics and the canonical env reference (T4.3, in `deploy.md`).

## References
- Brief: Mandatory Deliverables (README, setup guide, architecture explanation, hosting/URLs).
- HLD (all), clarifications, `docs/deploy.md` (canonical env reference), `.env.example` (var list).
- Mermaid (GitHub-native rendering) for all diagrams.

## Roadmap (high-level)
1. Finalize README setup guide + test logins + Live URLs section (fill real URLs from T4.3 deploy).
2. Add the README **Configuration** subsection (key env vars + secret flags) linking to the canonical
   `deploy.md` reference and `.env.example`.
3. Author the Mermaid diagrams (workflow state, auth sequence, deployment topology; optional Clean
   Arch layers) and embed them in HLD/README/deploy as mapped in Scope.
4. Reconcile the architecture docs with the actual implementation (note as-built deviations/tradeoffs).
5. Verify every documentation link + each Mermaid block renders; tidy the backlog (resolve B-030).

## Acceptance Criteria (Definition of Done — also workflow.md §6)
- [ ] README explains running backend, frontend, DB setup, and lists working test accounts.
- [ ] README **Configuration** subsection lists the key env vars, flags secrets, and links to the
      canonical `deploy.md` reference + `.env.example` (no duplicated/contradictory env tables).
- [ ] Live URLs section present (frontend, API, docs, repo, deploy notes).
- [ ] Diagrams present and render as Mermaid: request workflow (state), auth (sequence), deployment
      topology (flowchart) — placed per Scope; RBAC shown as a table.
- [ ] Architecture explanation matches the built system; assumptions/tradeoffs stated.
- [ ] All doc links resolve; backlog current (B-030 collision resolved).

## Test Expectations
- Manual: follow the README from scratch and confirm the steps work (capture in PR evidence).
- Manual: confirm each Mermaid block renders on GitHub (no syntax errors) and every doc link resolves.

## Best-Practices Checklist (subset)
- [ ] Docs updated to match reality; no stale links; test creds clearly test-only.
